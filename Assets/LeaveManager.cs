using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LeaveManager : MonoBehaviour
{  
// Update is called once per frame
    //void Update()
    //{
    //    transform.rotation = Quaternion.Euler(transform.eulerAngles.x, Camera.main.transform.position.y - transform.position.y, transform.eulerAngles.z);
    //}
}
